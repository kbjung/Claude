# 구독_관리 폴더 작업 규칙

클로드 등 구독형 서비스의 사용량·비용을 트래킹하는 자료를 보관하는 폴더.
아래 규칙은 저장소 마스터 규칙(`GitHub\Claude\CLAUDE.md`, `사업/창업/CLAUDE.md`, `대학원/CLAUDE.md`)의
문서 관리 규칙을 이 폴더에도 동일하게 적용한 것.

---

## ★ 문서 생성 규칙 — md를 원본으로, html은 스크립트로 생성

**html을 직접 작성·편집하지 않는다.** md를 원본으로 쓰고, 아래 도구로 html을 생성한다.

```
python3 "_도구/build_doc.py" 문서.md          # 같은 폴더에 문서.html 생성
```

- 도구 위치: `구독_관리/_도구/build_doc.py` (창업·대학원 사본과 동일 스크립트 — 스타일 바꿀 때만 함께 동기화)
- 의존성: `pip install markdown --break-system-packages` (스크립트가 자동 설치 시도)

## ★ 파일명 규칙

- 형식: `{문서명}_YYYYMMDD_HHMM_(claude|제이).{확장자}`
- 시간은 **한국시간(KST) 기준** — `TZ='Asia/Seoul' date` 명령으로 확인 후 사용
- md와 html은 **같은 버전명(같은 날짜·시각)으로 함께 저장**

## ★ 버전 관리 — 업데이트 시 이전 버전은 `99 이전 버전/`으로 이동

내용을 업데이트할 때마다:
1. 새 버전명(`{문서명}_YYYYMMDD_HHMM_claude`)으로 md 작성 → `build_doc.py`로 html 재생성
2. **기존(구) 버전의 md·html을 모두 `99 이전 버전/` 폴더로 이동** — 폴더에는 항상 최신 버전만 남긴다
3. 다른 명칭(`이전 버전`, `_이전 버전`, `99 과거 버전` 등) 사용 금지 — 저장소 전체 통일 규칙

---

## ★ 확인된 사실 — 사용 크레딧과 한도의 관계 (2026-08-27 실사용 확인)

**사용 크레딧은 5시간 세션 한도뿐 아니라, 주간(전체 모델) 한도가 100% 소진된 뒤에도 추가 사용을 가능하게 한다.**

- Anthropic 공식 문서(「Manage usage credits」)는 세션 한도 기준으로만 서술되어 있어 08-25 업데이트 시점에는 이 부분이 불확실로 남아 있었음
- 2026-08-27, 마루호 지원서 마감(08-28 14:00) 임박 작업 중 주간 한도 100% 소진 → 사용 크레딧 $50 구매·활성화로 작업을 계속 진행할 수 있음을 실제로 확인
- 08.27~28 이틀간 실제로는 월 지출한도($50)를 **두 차례(총 $99)** 소진 — 마감 직전 며칠에 코덱스 교차검토·원문대조 등 무거운 작업이 몰리면 하루 지출한도를 넘겨 반복 소진될 수 있음
- **활용법:** 마감이 임박한 날 주간 한도가 다 찼다면, 주간 리셋(토요일 오전)을 기다리지 않고 크레딧을 구매해 그날 안에 작업을 끝낼 수 있음. 다만 지출한도에 다시 도달하면 재구매가 필요하므로, 마감 직전 며칠은 지출한도를 미리 올려두거나 진행 상황을 체크하며 대응

---

## 변경 이력

| 날짜 | 변경 내용 |
|------|----------|
| 2026-08-22 | 폴더 신설. 클로드 사용량 트래킹 로그 최초 정리(`클로드_사용량_트래킹_로그_20260822_2322_claude.md/html`) |
| 2026-08-22 23:26 | 문서 버전 관리 규칙(파일명 규칙 + 업데이트 시 `99 이전 버전/` 이동) 정립 — 저장소 마스터 규칙과 통일 |
| 2026-08-25 17:36 | 08.25 사용량·작업 내역 반영해 새 버전 발행(`..._20260825_1736_claude.md/html`). 규칙대로 구버전(`..._20260822_2322`)을 `99 이전 버전/`으로 이동 — 이 규칙의 첫 실제 적용 사례 |
| 2026-08-27 23:26 | 「사용 크레딧이 주간 한도 소진 후에도 적용됨」을 실사용으로 확인 — 확인된 사실 섹션 신설. 08.27 사용량 반영해 새 버전 발행(`..._20260827_2326_claude.md/html`), 구버전(`..._20260825_1736`)을 `99 이전 버전/`으로 이동 |
| 2026-08-30 23:53 | 08.27~28 이틀 작업(마루호 지원서 전 구간 완성 + 코덱스 2라운드 + hwp 4개 버전 대조) 상세 내역과 최종 비용($50×2회=$99) 반영해 새 버전 발행(`..._20260830_2353_claude.md/html`). 「확인된 사실」에 이틀 반복 소진 사례 추가. 구버전(`..._20260827_2326`)을 `99 이전 버전/`으로 이동 |
| 2026-08-30 23:58 | 08.30 콜스위크 부스 준비 문서 작업(living doc 10차 갱신, 카피 문구 5회 재번역, PDF/PPT 분석·렌더링 검증 다회) 반영, 월 누적 크레딧이 $100.50/100%로 한도 도달한 것 확인. 「확인된 사실」에 "요약 텍스트 붙여넣기 자체도 사용량을 늘림" 관찰 추가. 새 버전 발행(`..._20260830_2358_claude.md/html`), 구버전(`..._20260830_2353`)을 `99 이전 버전/`으로 이동 |
