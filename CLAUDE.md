# Claude 모델 선택 가이드

작업 유형에 따라 Sonnet 5를 기본값으로 쓰고, 전략적 판단이 필요한 구간에서만 Opus 5로 스팟 전환한다. Haiku 4.5는 가벼운 단순 조회에만 사용한다.

---

## 1. Sonnet 5 — 기본값 (대부분의 작업)

| 작업 유형 | 예시 | 비고 |
|---|---|---|
| 정형화된 문서 변환 | 회의록 정리, STT(클로바노트 등) 교정 | `meeting-notes` 스킬 |
| 논문 번역/대역문서 | 영한 병렬 대역 HTML 생성 | `paper-translator` 스킬 |
| 코드·자동화 개발 | 파이프라인 스크립트, Cowork 스킬 빌드 | |
| 다중 문서 참조 작업 | 긴 자료·여러 문서를 동시에 열어봐야 하는 작업 | context window가 Opus보다 넉넉함 |
| 일반 개발 작업 | 홈페이지, 프론트엔드 샘플 제작 | |

**Why:** 위 작업들은 형식·규칙이 이미 정해져 있거나(스킬 기반), 실행 자체가 핵심이라 판단의 깊이보다 처리량·컨텍스트 용량이 중요하다.

---

## 2. Opus 5 — 전략적 전환 구간에만 스팟 사용

| 작업 유형 | 예시 | 전환 신호 |
|---|---|---|
| 최초 기획/로드맵 수립 | 전체 전략 구조 설계, 사업 방향 뼈대 잡기 | "처음부터 판을 짜야 하는가" |
| 복합 판단 종합 | 규제 해석, IP 전략, 사업 방향 결정 | 여러 자료를 종합해 결론을 내야 하는가 |
| 고비용 의사결정 | 되돌리기 어렵거나 실수 비용이 큰 판단 구간 | 틀렸을 때 손실이 큰가 |
| 다중 피드백 통합 수정 | 지원서·보고서 등에 사용자 수정본 + 코덱스 등 교차검증 피드백을 동시에 반영 | 소넷이 반영 누락·양식 훼손을 반복하는가 (2026-08-25 확인) |

**Why:** 이 구간은 처리량이 아니라 판단의 정확도가 핵심이며, 잘못된 판단의 비용이 모델 전환 비용보다 훨씬 크다.

**전환 판단 체크리스트** (하나라도 해당하면 Opus 고려)

- 이 결정이 틀리면 되돌리기 어렵거나 비용이 큰가
- 서로 다른 자료·근거를 종합해 하나의 결론을 내려야 하는가
- 지금까지 없던 새로운 전략/구조를 처음부터 설계하는가
- 이미 작성된 문서에 여러 출처의 피드백(사용자 수정본, 코덱스 크로스체크 등)을 동시에 반영해야 하는가 — 소넷은 이런 다중 피드백 반영에서 일부 항목을 놓치거나 기존 양식을 훼손하는 경우가 있음

### 다중 피드백 반영(문서 리비전) 시 사용량 절약 팁 (2026-08-25 정립)

지원서·보고서처럼 이미 작성된 문서에 여러 소스의 피드백을 반영하는 단계는 "여러 자료를 종합해 결론을 내야 하는" 판단 작업이라 Opus 재전환이 맞다. 다만 Opus에게 **문서 전체를 다시 쓰게** 하면 사용량이 빨리 소진되므로, 아래처럼 역할을 나눈다.

1. **Opus**: 여러 출처의 피드백(사용자 수정본, 코덱스 크로스체크 등)을 종합해 "무엇을 어떻게 고칠지" **번호 매긴 수정 지시 리스트**만 작성
2. **Sonnet**: 그 리스트를 그대로 받아 항목별로 문서에 적용하고, 마지막에 "항목별 반영 여부" 표로 스스로 대조·보고
3. 필요 시 코덱스 등으로 재크로스체크 → 놓친 항목만 다시 1)로

**Why:** 소넷이 놓치는 원인의 상당수는 피드백이 뭉뚱그려서 한 번에 주어지기 때문. 지시를 항목별로 쪼개고 반영 여부를 스스로 대조하게 하면 소넷 단계에서도 누락률이 줄어, Opus는 "판단"에만 쓰고 "반영 작업"은 계속 Sonnet에 맡길 수 있다.

---

## 3. Haiku 4.5 — 단순 조회 (있다면)

| 작업 유형 | 예시 |
|---|---|
| 가벼운 정보 조회 | 제품 정보, 생활 팁 등 |

**Why:** 판단이나 종합이 필요 없는 즉답형 질문에는 가장 가벼운 모델로 충분하다.

---

## 작업 흐름

```
Sonnet 5로 시작
   │
   ▼
전략적 판단이 필요한 구간 발생?
   │
   ├─ No  → Sonnet 5 계속 진행
   │
   └─ Yes → Opus 5로 전환 (해당 판단 구간만)
              │
              ▼
        판단/결론 도출 완료
              │
              ▼
        Sonnet 5로 복귀 (실행·문서화·후속 작업)
```

**원칙:** Opus는 "판단이 필요한 순간"에만 켜고, 판단이 끝나면 곧바로 Sonnet으로 되돌아온다. 전체 작업을 Opus로 유지하지 않는다.

---

**★ 세션 중 모델 자동 조정 규칙 (2026-09-05 정립, 창업·대학원 CLAUDE.md와 공통)**

세션을 어느 모델로 시작했든, 작업 성격이 모델과 맞지 않으면 아래대로 스스로 조정한다.
제이가 매번 모델을 지정하지 않아도 되게 하는 것이 목적.

| 세션 모델 | 작업 성격 | 처리 |
|---|---|---|
| Opus 5 | 정형 작업 | **Sonnet 서브에이전트로 위임**하고 Opus는 결과 검증만 (Opus 사용량 절약) |
| Opus 5 | 전략 판단 | Opus가 직접 수행 |
| Sonnet 5 | 정형 작업 | Sonnet이 직접 수행 (위임 불필요) |
| Sonnet 5 | 전략 판단 | ① 되묻기 없이 한 번에 끝나는 분석·설계 산출물 → **Opus 서브에이전트로 위임**<br>② 주고받으며 판단을 좁혀야 하는 건 → **"Opus로 전환 후 진행"을 제이에게 먼저 요청**하고 대기 |

- **정형 작업** = 문서 변환(md↔html·회의록·랩미팅 노트·STT 교정), 논문 번역, 파일·폴더 정리, 경로·파일명 갱신, 엑셀 반영, 정해진 양식 채우기, 코드·자동화 개발
- **전략 판단** = 위 「전환 판단 체크리스트」 4개 항목 중 하나라도 해당하는 작업
- 서브에이전트는 **대화 맥락을 물려받지 못한다** — 근거 문서 경로·작업 규칙·확정 사실을 프롬프트에 모두 적어 보낼 것
- 서브에이전트 결과는 **상위 모델이 반드시 검증**한 뒤 보고 (파일 존재·내용 대조·수치 확인)
- 이 규칙은 지침이며 강제되지 않는다. 성격이 애매하면 위임하지 말고 제이에게 확인

---

## 빠른 참고표

| 질문 | Yes → | No → |
|---|---|---|
| 이미 정해진 형식/스킬을 따르는 작업인가? | Sonnet 5 | 아래로 |
| 여러 자료를 종합해 새로운 판단을 내려야 하는가? | Opus 5 | Sonnet 5 |
| 여러 소스 피드백을 기존 문서에 반영하는데 소넷이 자꾸 놓치는가? | Opus 5 (수정 지시서만 작성 후 Sonnet에 위임) | Sonnet 5 |
| 판단 없이 바로 답할 수 있는 단순 질문인가? | Haiku 4.5 | Sonnet 5 |

---

## 변경 이력

| 날짜 | 변경 내용 |
|------|----------|
| 2026-07-31 | 최초 작성 — 창업·대학원 CLAUDE.md에 마스터로 반영 |
| 2026-08-25 | 「다중 피드백 통합 수정」 트리거 추가 — 지원서 등 리비전 단계에서 소넷이 반영 누락·양식 훼손을 반복하는 문제 확인 후, Opus는 "수정 지시 리스트"만 작성하고 실제 반영은 Sonnet에 위임하는 절약 패턴 정립 (창업·대학원 CLAUDE.md에도 동일 반영) |
| 2026-09-05 | 「세션 중 모델 자동 조정 규칙」 신설 — 세션 모델과 작업 성격이 어긋날 때 서브에이전트 위임(Opus→Sonnet, Sonnet→Opus 단발 산출물) 또는 제이에게 모델 전환 요청. 창업·대학원 CLAUDE.md와 동일 반영 |
